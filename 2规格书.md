# 流水账 v3 开发规格书

> 技术方案，对应 `1需求.md`。以下描述与实际代码一致。

---

## 一、技术架构

- 单文件 HTML + CSS + JS（约 3100 行），不引入框架
- 数据持久化：localStorage + sync-server 落盘
- AI：DeepSeek API（`deepseek-chat`），`response_format: { type: 'json_object' }`
- UI 风格：像素风，Press Start 2P 字体 + CSS `box-shadow` 边框

---

## 二、数据模型

### 2.1 TimeRecord

```javascript
{
  id: string,           // 8 位随机字符 [a-z0-9]，genId() 生成
  date: "YYYY-MM-DD",   // 记录归属日期
  start: "HH:MM",       // 开始时间，24 小时制
  end: "HH:MM",         // 结束时间。跨天睡眠时 start > end（如 "23:00" > "07:00"）
  activity: string,     // 活动描述，AI 精简后的核心动作词
  category: string,     // "工作" | "生活-身体" | "生活-精神" | "成长"
  duration: number,     // 分钟数，calcDuration(start, end) 计算
  energy: number,       // -3 到 +3，能量（-3=非常掉电, 0=持平, +3=非常回血）
  flow: number,         // -3 到 +3，投入感（-3=非常不投入, 0=一般, +3=非常投入）
  dimension: string,    // "sleep" | "body" | "spirit" | "work" | "commute" | "growth"
  breakdown: [          // 子活动拆分，6维度分析数据源
    { activity: string, duration: number, dimension: string }
  ],
  corrected: boolean,   // 用户纠正过 category/energy/flow → 纳入 few-shot
  linkedTaskId: string, // 关联的待办任务 ID，空字符串 = 无关联
  createdAt: number,    // Date.now()

  // 以下为运行时标记，不参与 AI 序列化
  _eTouched: boolean,   // 是否设置过 energy（区分"从未评价"和"明确设为0"）。AI 回填或用户手动设置时均设为 true
  _fTouched: boolean,   // 是否设置过 flow，同上
}
```

### 2.2 ChatMessage

```javascript
{
  role: "user" | "assistant",
  content: string,
  timestamp: number,
}
```

### 2.3 Task（四象限+待办）

```javascript
{
  id: string,           // "t" + 递增数字，nid() 生成
  name: string,         // 任务名称
  zone: string,         // 所属位置："q1"|"q2"|"q3"|"q4"|"todo"
  startAt: number|null, // 计时开始时间戳
  elapsed: number,      // 已计时毫秒数
  running: boolean,     // 是否计时中
}
```

### 2.4 DailySummary / AiLog

```javascript
{
  date: "YYYY-MM-DD",
  content: string,       // AI 生成的文字内容
  generatedAt: number,   // 生成时间戳
  trigger: "manual" | "auto" | "keyword",
}
```

### 2.5 localStorage Keys

| Key | 内容 | 结构 |
|-----|------|------|
| `qt_v3` | 四象限+待办 | `{ tasks: Task[], done: [] }` |
| `qt_timelogs` | 时间记录 | `{ "YYYY-MM-DD": TimeRecord[] }` |
| `qt_chat_history` | AI 对话 | `{ "YYYY-MM-DD": ChatMessage[] }` |
| `qt_daily_summaries` | Tab 1 总结 | `{ "YYYY-MM-DD": DailySummary }` |
| `qt_ai_logs` | Tab 2 分析日志 | `{ "YYYY-MM-DD": DailySummary }` |
| `qt_diary` | 用户日记 | `{ "YYYY-MM-DD": "日记文字" }` |
| `qt_ds_key` | API key | string（直接存，非 JSON） |

### 2.6 数据安全

```javascript
// safeSave：每次写前备份到 _backup
function safeSave(key, data) {
  try { const prev = localStorage.getItem(key); if (prev) localStorage.setItem(key + '_backup', prev); } catch (e) {}
  localStorage.setItem(key, JSON.stringify(data));
}

// safeLoad：解析失败 → 从 _backup 恢复，不静默覆盖
function safeLoad(key, fallback) {
  try { const raw = localStorage.getItem(key); if (raw === null) return fallback; return JSON.parse(raw); }
  catch (e) {
    const backup = localStorage.getItem(key + '_backup');
    if (backup) { try { const recovered = JSON.parse(backup); localStorage.setItem(key, backup); return recovered; } catch (e2) {} }
    return fallback;
  }
}
```

---

## 三、数据迁移（v2 → v3）

`migrateV2toV3()` — 在 `init()` 开头执行一次：

1. 检查 `qt_v3_migrated` 标记，已迁移则跳过
2. 读取旧 `qt_timelogs`，兼容两种 v2 格式：直接数组 或 `{ entries: [...] }`
3. `生活·身体` → `生活-身体`，`生活·精神` → `生活-精神`
4. 新字段默认值：`energy: 0, flow: 0, corrected: false, linkedTaskId: "", dimension: ""`
5. 删除 v2 旧字段（`startTime`, `endTime`, `durationMin`, `linkedTaskName`, `rawInput`）
6. 设置迁移标记 `qt_v3_migrated = '1'`

注意：dimension 字段迁移后为空字符串，代码在 `computeDailyStats` 中有 category→dimension 的兜底推断逻辑。

---

## 四、AI 集成

### 4.1 System Prompt 组装

`buildSystemPrompt(dateStr)` 返回完整 prompt，由五部分拼接：

1. **主提示词**（内联常量）— 角色定义、核心原则、四大分类、时间质量、日期/时间识别、工作流、输出格式（含 batch）、六维度归类、边界规则
2. **日期提示** — `今天的日期: ${dateStr}（用户可能描述其他日期如"昨天""上周五""7月15日"等，以用户实际描述的日期为准）`

> `dateStr` 始终为真实今天（`todayStr()`），不受中栏浏览日期影响。「昨天/前天/具体日期」的换算由 AI 在 prompt 中按优先级处理：跨天睡眠永远归入醒来当天（最高优先级），其余记录按用户描述的日期换算。
3. **已有记录** — 收集 dateStr 前后各 1 天 + 最近 3 天有记录的日期，格式化列出所有记录（id/时间/活动/分类/能量/投入）
4. **最近对话** — 跨日期收集最近 3 天的对话，取最近 20 条
5. **Few-shot** — 收集所有 `corrected: true` 的记录，按 `createdAt` 降序取最近 20 条

### 4.2 AI 返回格式

```json
// 单个操作
{ "action": "create", "data": { "date": "...", "start": "...", "end": "...", "activity": "...", "category": "...", "energy": -1, "flow": 2, "dimension": "work" }, "reply": "确认消息" }

// 批量操作
{ "action": "batch", "items": [{ "action": "create", "data": {...} }, { "action": "update", "id": "xxx", "data": {...} }], "reply": "确认消息" }

// 反问
{ "action": "ask", "reply": "大概几点到几点？" }

// 清空上下文（不需要 data/id）
{ "action": "clear_context", "reply": "上下文已清空" }
```

### 4.3 API 调用参数

```javascript
// 主调用
{ model: 'deepseek-chat', messages: [...], response_format: { type: 'json_object' }, temperature: 0.7, max_tokens: 4000 }

// 计时器补全调用 — max_tokens: 300
// My Day 分析日志调用 — max_tokens: 1000
```

### 4.4 Action 执行器（executeAction）

| action | 代码行为 |
|--------|---------|
| batch | 遍历 items，逐个执行子 action（create/update/delete），最后 renderMidCol |
| create | genId() → calcDuration() → 默认值 → push → 按 isMainSleep + start 排序 → saveTimelogs() → renderMidCol |
| update | 按 id + date 查找 → 覆盖非空字段 → 时间变则重算 duration → category/energy/flow 变则 corrected=true → 排序 → saveTimelogs() → renderMidCol |
| delete | 按 id + date 查找 → splice → saveTimelogs() → renderMidCol |
| ask | 仅返回 reply 文本 |
| clear_context | 保留当天最近 10 条对话，其余清除 |

### 4.5 关键词触发

- **SLEEP_KEYWORDS**：`["睡了", "晚安", "一天结束", "结束了", "收工"]`
- 命中 → 先执行 AI 返回的 create/update

### 4.6 六维度归类（AI 侧）

AI 在 create 时输出 `dimension` 字段，语义判断规则：

| dimension | 含义 | 示例 |
|-----------|------|------|
| sleep | 睡眠及相关状态 | 睡觉、眯了、午休、赖床、回笼觉 |
| body | 维持身体运营（非睡眠） | 吃饭、洗漱、穿衣、煮饭、打扫 |
| spirit | 精神放松、娱乐 | 刷抖音、看小说、社交、聚餐 |
| work | 兑换经济的工作 | 开会、写文档、上班 |
| commute | 通勤 | 通勤、坐地铁、开车上班 |
| growth | 自我提升 | 阅读、学技能、健身、复盘、AI 产品开发 |

关键原则：
- 维度 ≠ 分类。通勤 category=工作，dimension=commute
- 睡眠维度看状态不看关键词。"眯了"不含"睡"字，但 AI 理解这是睡眠状态 → sleep
- 代码不做语义判断，只读 `r.dimension` 字段。缺失时按 category 兜底推断

---

## 五、计算引擎

### 5.1 computeDailyStats(dateStr)

```javascript
// 返回值
{
  date,                    // 日期
  byCategory,              // { "工作": min, "生活-身体": min, "生活-精神": min, "成长": min }
  dimensions,              // { sleep: min, work: min, commute: min, body: min, spirit: min, growth: min }
  breakdown,               // { sleep: { act: min }, work: { act: min }, commute: { act: min }, body: { act: min }, spirit: { act: min }, growth: { act: min } }
  energyDist,              // { charge: min, neutral: min, drain: min }
  flowDist,                // { high: min, low: min }
  totalMin,                // 总分钟数
}
```

### 5.2 维度归类逻辑

直接从每条记录的 `breakdown` 字段读取。每条 breakdown 项的 `dimension` 决定归属，`activity` + `duration` 累积到对应维度的明细中。无 breakdown 时兜底用记录本身的 activity/duration/dimension。

### 5.3 质量分布

- energyDist：energy > 0 → charge, = 0 → neutral, < 0 → drain
- flowDist：flow ≥ 3 → high, < 3 → low

### 5.4 跨天时长

```javascript
function calcDuration(start, end) {
  const startMin = sh * 60 + sm;
  const endMin = eh * 60 + em;
  if (startMin >= endMin) return (24 * 60 - startMin) + endMin;
  return endMin - startMin;
}
```

### 5.5 活动名合并

展示活动明细时，对 breakdown 内的活动名做智能合并（字符集重叠度算法）。由于 AI 输出的 breakdown 已有较好的活动名一致性，此合并为保底逻辑。

---

## 六、UI 组件

### 6.1 Tab 1 布局

```
三栏 flex row，左右分隔条可拖拽
左栏 ~30%：四象限(2×2 grid) + 分隔条 + 今日待办
中栏 ~38%：日期导航 + 环形图+6维度 + 时间线(独立滚动)
右栏 flex：AI 对话框(聊天气泡+输入框)
```

### 6.2 日期导航

- `navDate(dir)`：-1 前一天，+1 后一天，0 回今天
- 中间文字可点击弹出 mini 日历（`toggleDatePicker`）
- [今天] 按钮：当天隐藏，非当天显示
- 日历支持翻月，有记录的日期标 `.has-done`

### 6.3 环形图

- Canvas 绘制，120×120 CSS px，Retina 适配
- 外径 50px，内径 30px（环形），4 色分段
- 中心显示总时长（`fmtHours`）
- 右侧 legend：短分类名 + 时长
- 无数据时灰色空心环

### 6.4 时间线

排序规则：
1. 跨天睡眠（`isMainSleep(r)` = dimension==='sleep' && start > end）排最前
2. 其余按 `start` 升序

每条行内容：
- 时间区间 + 活动名 + 关联图标(🔗) + 时长 + 分类标签(可点击) + 删除按钮(✕)
- 下方质量行：♥️ 标签 + 🎯 标签（睡眠行无质量行）

**双击**：活动名 → 内联编辑；时间区间 → 内联编辑（`HH:MM-HH:MM` 格式校验）

**拖拽排序/合并**：
- 拖到行边缘 → 排序（src 移到 tgt 位置）
- 拖到行中心 → 合并（时间范围扩展、时长相加、保留较长名、删除 src）

### 6.5 质量标签

**初始状态**：只显示 ♥️ 或 🎯 图标，不显示文字（`_eTouched` 为 falsy）

**点击图标** → 弹出 `.qi-popup`（fixed 定位，z-index: 9999）：
- 行 1：主选项（回血/持平/掉血 或 投入/一般/不投入）
- 行 2：子选项（轻微/中度/非常 或 轻微/一般/非常）
- 当前值非零时对应选项高亮（`.sel`）
- 选"持平"或"一般"→ 立即保存关闭，值设为 0
- 选其他主选项 → 需再选子选项 → 自动保存关闭
- 弹窗超出屏幕下方时翻到上方

**保存后**：
- 设置 `rec._eTouched = true` 或 `rec._fTouched = true`
- 显示格式：程度在前，状态在后（如"中度掉血""一般投入"）
- 持平/一般：显示"持平""一般"

**取值映射**：
```javascript
// 显示
energyMain(val): >0→回血, <0→掉血, =0→持平
energySub(val):  |1|→轻微, |2|→中度, |3|→非常
flowMain(val):   >0→投入, <0→不投入, =0→一般
flowSub(val):    |1|→轻微, |2|→一般, |3|→非常

// 还原
qValFromLabels(type, main, sub):
  signMap = energy{回血:1,持平:0,掉血:-1} / flow{投入:1,一般:0,不投入:-1}
  absMap  = energy{轻微:1,中度:2,非常:3} / flow{轻微:1,一般:2,非常:3}
  return sign × abs
```

---

## 七、Tab 2 My Day

### 7.1 日历

- 独立状态变量：`calYear`, `calMonth`, `calSelected`
- 翻月、选日期、今天按钮
- 有记录或有日记的日期标圆点
- 选日期 → `calSelect(ds)` → 加载日记 + 数据面板 + AI 日志

### 7.2 日记

- 点击预览区 → modal（textarea）
- 关闭 modal 自动保存
- 支持导出全部日记为 Markdown

### 7.3 客观数据面板（renderDataPanel）

- 6 维度条形图（最长维度为 100% 基准）
- 工作子项：充电时段（energy > 0）、耗电时段（energy < 0）
- 精神子项：高投入低能量时段（flow ≥ 3 && energy < 0）
- 能量分布（充电/中性/耗电，百分比+条形图）
- 投入分布（高投入 flow ≥ 3 / 低投入，百分比+条形图）
- 无数据时显示空状态

### 7.4 AI 分析日志（renderAiLogPanel）

- 先检查 `aiLogs[dateStr]` 缓存
- 无缓存 → 调 DeepSeek API，传入时间数据 + 日记原文 + 分析提示词
- 独立 system prompt（150-250 字，交叉验证 + 质量判断 + 复盘 + 建议）
- 不强制 `response_format: json_object`
- 生成后缓存

---

## 八、Tab 3 阿药

- 关于页 + 功能说明 + 联系方式
- 「AI 日记」按钮：调 renderAiLogPanel 生成四板块日记
- 「清理数据」按钮：调 cleanupOldData()
- API key 配置

---

## 九、计时器联动

左栏与中栏时间线的唯一联动方式：

1. 点 ▶ → `_activeTimers[id] = { taskName, startTime, startMs }` → 时间线顶部出现「进行中」条目
2. 点 ⏸（停止计时）→ createRecord（含 `linkedTaskId: t.id`，category/energy/flow 留空）→ 调 AI 补充分类和质量（独立 API 调用，简单 prompt）→ `t.elapsed = 0`
3. 点 ✓（完成）→ 只移除任务，不创建时间线记录。如计时器在运行则丢弃计时数据
4. 点 ✗ → 取消计时，移除进行中条目，保留任务

---

## 十、动态学习

- AI 给出分类/质量 → 用户通过时间线交互纠正（改分类弹窗、拖质量标签）→ `corrected = true`
- AI update action 改了 category/energy/flow → `corrected = true`
- `buildFewShot()`：遍历所有 timelogs，收集 `corrected: true` 的记录，按 `createdAt` 降序取最近 20 条
- 格式化为提示词文本嵌入 system prompt

---

## 十一、自动行为

### 11.1 数据清理（J2）

- init() 时检查：当前时间 ≥ 6:00 且今天尚未清理 → 执行
- 删除 10 天前的 timelogs、chatHistory、aiLogs、diaries
- 记录 `qt_last_cleanup`

---

## 十二、Sync-Server 集成

```javascript
const SYNC_KEYS = new Set([
  "qt_v3", "qt_timelogs", "qt_chat_history",
  "qt_ai_logs", "qt_diary", "qt_ds_key"
]);
```

- 包装 `localStorage.setItem`，写入 SYNC_KEYS 自动触发防抖同步（30 秒）
- 页面隐藏/关闭时立即同步
- 启动时 localStorage 为空 → 尝试从 `http://localhost:9879/{app}/{key}` 恢复

---

## 十三、配置常量

```javascript
const CONFIG = {
  MAX_RECORD_DAYS: 10,
  CLEANUP_TIME: "06:00",
  MAX_CHAT_CONTEXT: 20,
  MAX_CHAT_CONTEXT_OTHER: 10,
  FEWSHOT_SAMPLE_SIZE: 20,
  ID_LENGTH: 8,
  SLEEP_KEYWORDS: ["睡了", "晚安", "一天结束", "结束了", "收工"],
};
```

---

## 十四、边界情况

| 情况 | 处理 |
|------|------|
| AI 返回非 JSON | console.error 原始内容，抛错提示重试 |
| 跨天睡眠 | start > end → calcDuration 自动处理。date 永远归入醒来当天（即 end 所在日期），此规则在 AI prompt 中优先级最高，不受「昨天/前天」等相对日期词影响 |
| localStorage 加载失败 | 从 _backup 恢复 |
| dimension 缺失 | computeDailyStats 按 category 兜底推断 |
| 质量从未设置 | _eTouched/_fTouched 为 false，只显示图标不显示文字 |
