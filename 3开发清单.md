# 流水账 v3 开发清单

> 已完成项标记 ✅，进行中标记 🔄，未开始无标记。

---

## 阶段 A：数据层

### A1. localStorage 读写 ✅
- 定义 7 个存储 key：`qt_v3`, `qt_timelogs`, `qt_chat_history`, `qt_daily_summaries`, `qt_diary`, `qt_ai_logs`, `qt_ds_key`
- 每个 key 实现 load/save 函数（safeLoad/safeSave），save 前备份到 `_backup`
- load 失败时从 `_backup` 恢复，不静默覆盖
- 注册到 sync-server 的 SYNC_KEYS
- 状态：**已完成**

### A2. 数据迁移（v2 → v3） ✅
- migrateV2toV3() 在 init() 开头执行，检查 `qt_v3_migrated` 标记
- `生活·身体` → `生活-身体`，`生活·精神` → `生活-精神`
- 新字段默认值：`energy: 0, flow: 0, corrected: false, linkedTaskId: "", dimension: ""`
- 兼容 v2 两种格式：直接数组 或 `{ entries: [...] }`
- 状态：**已完成**

---

## 阶段 B：计算引擎

### B1. computeDailyStats ✅
- 4 分类汇总（byCategory）
- 6 维度拆分：优先读 `r.dimension`，缺失时按 category 兜底推断
- 维度内活动分组（breakdown），含智能合并（字符集重叠度算法）
- 质量分布（energyDist: charge/neutral/drain, flowDist: high/low）
- 跨天睡眠特殊处理（start > end 时 calcDuration 自动处理）
- fmtDuration / fmtHours 辅助函数
- 状态：**已完成**

---

## 阶段 C：AI 集成

### C1. 对话历史管理 ✅
- getChatHistory(dateStr) / addChatMessage(role, content)
- cropChatHistory → 合并到 clear_context action 中
- 状态：**已完成**

### C2. few-shot 构建 ✅
- buildFewShot()：遍历所有 timelogs，收集 `corrected: true` 的记录
- 按 createdAt 降序，取最近 20 条
- 格式化为提示词文本
- 状态：**已完成**

### C3. buildSystemPrompt + callDeepSeek ✅
- buildSystemPrompt(dateStr)：主提示词 + 今天日期 + 已有记录（多日期） + 最近对话（跨日期） + few-shot
- callDeepSeek(dateStr, userInput)：调 DeepSeek API，`response_format: { type: 'json_object' }`
- 提示词已在代码中更新为最终版（含 dimension、batch、边界规则）
- 状态：**已完成**

### C4. action 执行器 ✅
- executeAction(result)：支持 batch / create / update / delete / summary / clear_context / ask
- create：genId() → calcDuration() → 默认值 → push + sort（按 isMainSleep + start）
- update：按 id 查找 → 覆盖非空字段 → 重算 duration / 标 corrected
- delete：按 id 删除
- summary：computeDailyStats → 第二次调 AI → 存 dailySummaries
- clear_context：保留最近 10 条对话
- 状态：**已完成**

---

## 阶段 D：Tab 1 左栏

### D1. 四象限 + 待办 ✅
- 从 v2 复用：四象限 grid、拖拽排序（dz.over/dz.leave/dz.drop）、待办添加/删除
- 数据存 `qt_v3`，load/save 函数包装
- 状态：**已完成**

### D2. 计时器联动 ✅
- 点 ▶：记录到 `_activeTimers`，时间线显示「进行中」条目
- 点 ⏸（停止计时）：createRecord（缺 category/energy/flow）→ tryCallAIForComplete 补全 → `t.elapsed = 0`
- 点 ✓（完成）：只移除任务，不创建时间线记录
- 点 ✗：取消计时，移除进行中条目，保留任务
- `deleteTask` 清理 `_activeTimers` 防幽灵行
- `_eTouched`/`_fTouched` 在 AI 回填时同步设置，质量文字可正常显示
- `stopTickIf` 简化为单一条件判断
- ticker 每 500ms 更新显示
- 状态：**已完成**

---

## 阶段 E：Tab 1 中栏

### E1. 日期导航 ✅
- 左右箭头翻日（navDate），[今天] 按钮
- 中间日期可点击弹出 mini 日历选择器（toggleDatePicker）
- 日历翻月、标记有记录的日期
- 状态：**已完成**

### E2. AI 总结区 ✅
- renderSummaryBar(dateStr)：有总结显示段落，无总结显示占位 + 「总结」按钮
- triggerSummary()：手动触发总结生成
- 状态：**已完成**

### E3. 环形图 + 6 维度 ✅
- Canvas 绘制 4 分类环形图（Retina 适配，中心总时长）
- 右侧 legend：分类名 + 时长
- 下方 6 维度明细（含活动子项，智能合并）
- 无数据空状态
- 状态：**已完成**

### E4. 时间线 ✅
- 排序：跨天睡眠顶置，其余按 start 升序
- 每条：时段 + 活动 + 关联图标 + 时长 + 分类标签 + 删除按钮
- 质量标签（♥️ 🎯，可点击弹窗选择）
- 进行中条目
- 无记录空状态
- 状态：**已完成**

### E5. 中栏整体 ✅
- renderMidCol(dateStr)：日期导航 → 总结 → 环形图+6维度 → 时间线
- 状态：**已完成**

---

## 阶段 F：质量标签组件 ✅

### F1. 质量弹窗 + 交互 ✅
- 时间线每条渲染 ♥️ 和 🎯 两个图标
- 初始状态：仅图标（_eTouched/_fTouched 为 false 时不显示文字）
- 点击弹出 `.qi-popup`：两行选项（主选项 + 子选项）
- 持平/一般 → 点完立即保存
- 其他 → 选主 + 选子 → 自动保存
- doSave：qValFromLabels 转换 → 更新 record → saveTimelogs → renderMidCol
- 弹窗位置自适应（底部超出翻到上方）
- 状态：**已完成**

---

## 阶段 G：Tab 1 右栏（AI 对话框）

### G1. 对话 UI ✅
- 聊天气泡列表（aiChatMessages），独立滚动
- 输入框（textarea）+ 发送按钮
- Enter 发送，Shift+Enter 换行
- 状态：**已完成**

### G2. 发送 + 响应流程 ✅
- 用户输入 → addChatMessage('user') → callDeepSeek → executeAction → addChatMessage('assistant', reply) → 重渲染
- 错误处理：API 失败显示错误消息
- 状态：**已完成**

### G3. 关键词触发总结 ✅
- SLEEP_KEYWORDS / SUMMARY_KEYWORDS 命中 → 先执行 create → 再调 summary
- 状态：**已完成**

---

## 阶段 H：Tab 2 My Day

### H1. 左侧（日历 + 日记） ✅
- 日历组件：翻月、选日期、标记有数据日期
- 日记：预览 + modal 编辑 + 导出 Markdown
- 数据存 `qt_diary`
- 状态：**已完成**

### H2. 右侧左半：客观数据面板 ✅
- renderDataPanel(dateStr)：6 维度条形图 + 工作/精神子项 + 能量分布 + 投入分布
- 无数据空状态
- 状态：**已完成**

### H3. 右侧右半：AI 分析日志 ✅
- renderAiLogPanel(dateStr)：先查缓存在 `aiLogs`
- 无缓存 → 调 DeepSeek + 时间数据 + 日记 → 生成 → 缓存
- 无数据空状态
- 状态：**已完成**

---

## 阶段 I：动态学习 ✅

### I1. 纠正标记 ✅
- update action 改了 category/energy/flow → `corrected = true`
- 用户点质量弹窗改 energy/flow → `_eTouched/_fTouched = true`（但不一定 `corrected = true`，待确认）
- buildFewShot 收集 corrected 记录作为 AI 参考
- 状态：**已完成**

---

## 阶段 J：自动行为

### J1. 自动总结（23:55） ✅ → **已删除**
- setInterval 每分钟检查，到达 23:55 触发
- 当天已有总结则跳过
- _lastFired 防重复
- 状态：2026-08 删除今日总结功能时移除

### J2. 数据清理（6:00） ✅
- init() 时检查，删除 10 天前数据
- 记录清理日期 `qt_last_cleanup`，同天不重复
- 状态：**已完成**

---

## 阶段 K：收尾

### K1. Tab 3 阿药 ✅
- 关于页 + 功能说明 + 联系方式
- 「生成总结」按钮：手动并行触发 Tab 1 总结 + Tab 2 分析日志
- 「清理数据」按钮：手动触发 cleanupOldData()
- API key 配置区
- 状态：**已完成**

### K2. 横/竖分隔条 ✅
- 从 v2 复用拖拽逻辑
- 状态：**已完成**

### K3. 清理旧代码 ✅
- 删除 v2 中不再需要的函数和变量
- 状态：**已完成**

---

## 后续优化项（未排期）

- [ ] 活动名合并规则优化（当前字符集重叠算法对短名称可能误合并）
- [ ] 质量弹窗 `corrected` 标记：用户手动调整质量后是否应标 `corrected = true`
- [ ] 日记修改后 AI 分析日志的自动刷新（目前需手动切 Tab 触发）
- [ ] 时间线跨天拖拽排序支持
