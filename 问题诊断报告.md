# 流水账 — 线上问题诊断报告

## 相关路径

### 代码
| 文件 | 本地路径 | CVM 路径 |
|------|----------|----------|
| 前端 | `/Users/liujiaqi/Desktop/阿药的Obsidian/产品开发/流水账/index.html` | `/root/time-and-me/index.html` |
| 后端 | `/Users/liujiaqi/Desktop/阿药的Obsidian/产品开发/流水账/server.js` | `/root/time-and-me/server.js` |

### 数据
| 内容 | CVM 路径 |
|------|----------|
| 用户数据目录 | `/root/time-and-me/data/users/umrzp8uceb10b8355/` |
| 日记录文件 | `{用户目录}/YYYY-MM-DD.json`（如 `2026-08-08.json`） |
| 用户配置 | `{用户目录}/profile.json` |
| 任务数据 | `{用户目录}/tasks.json` |
| 备份文件 | `{用户目录}/YYYY-MM-DD.json.backup`（每次 writeJSON 自动生成） |

### 运行
| 内容 | 位置 |
|------|------|
| PM2 进程名 | `time-and-me` |
| CVM 端口 | `5202` |
| 访问入口 | `https://test.houdi.vip/time/`（nginx 反代 → `127.0.0.1:5202`） |
| 错误日志 | `/root/.pm2/logs/time-and-me-error.log` |
| 输出日志 | `/root/.pm2/logs/time-and-me-out.log` |
| SSH | `ssh root@175.178.165.219`，密钥 `~/.ssh/id_ed25519` |
| 部署文档 | `/Users/liujiaqi/Desktop/阿药的Obsidian/产品开发/流水账/部署.md` |

## 现象

1. **8/8 数据大量错乱**：AI 创建了正确的记录，但存盘后多条记录的时间被集体改错（洗澡和刷抖音的 start 都变成了 14:30），睡觉记录整条丢失。
2. **跨日期记录错位**：8/8 的睡觉和看小说记录被搬到了 8/9。
3. **validateCreates 误判**：AI 创建的「睡觉」「洗澡」被当作幻觉重试，用户原文是「睡的觉」「洗了个澡」，精确匹配不到。
4. **PM2 重启 12 次 / 37 小时**：服务频繁崩溃，无全局错误捕获。
5. **extractJson 解析失败 2 次**：返回的 JSON 内容合法但被拒绝，可能是不见字符导致。

## 根因分析

### 根因 1：saveDayToAPI 全量覆盖 + 前端不加载跨日期数据

**代码位置**：
- `index.html:1101-1113` saveDayToAPI — 用前端内存中的 `timelogs[date]` 全量 POST 到后端，后端直接替换该日期的所有记录。
- `index.html:1139-1149` loadAllData — 只加载 `currentDate` 的数据。
- `server.js:190-201` POST `/api/day/:date` — `records: records || existing.records`，传了 records 就全量替换。

**触发路径**：用户在 8/9 补记 8/8 的内容。AI 返回的 actions 包含 8/8 的新记录（下午/晚上部分），但不包含 8/8 早上另一轮对话创建的记录（早上部分不在当前上下文中）。`executeAction` 处理后调用 `saveDayToAPI("2026-08-08")`，此时 `timelogs["2026-08-08"]` 只有刚创建的那几条，没有早上那轮对话创建的数据。POST 到后端后，早上那轮对话的全部记录被覆盖丢失。

### 根因 2：updateRecord 擅自跨日期搬移记录

**代码位置**：`index.html:1452-1541` updateRecord 函数。

**逻辑**：当在当前日期 `dateStr` 找不到目标 id 时，遍历所有已加载的 timelogs 找。找到后，**把记录从旧日期删掉、加入当前日期、并设置 `record.date = dateStr`**。

**触发路径**：用户在 8/9 说「九点十五分醒的」，AI 发出 `update id="ma9xxgbj" end="09:15"`。这条记录的 id 存在于 `timelogs["2026-08-08"]`。updateRecord 在 8/9 找不到，跨日期搜索在 8/8 找到了，于是把整条记录搬到了 8/9。但用户只是要改 end 时间，不是要改日期。

**正确行为**：update data 里没有传 date 字段时，不应改变记录的日期。跨日期搜到的记录在原地更新即可，不要搬移。

### 根因 3：validateCreates 用 2-gram 精确匹配，太严格

**代码位置**：`server.js:436-452` validateCreates 函数。

**逻辑**：对 AI 创建的每条记录的 title，取所有连续 2 个字符的子串，检查是否有任意一个子串在用户原始消息中出现。全部不匹配 → 判定为幻觉，触发重试。

**问题**：用户说「10:15睡的觉」「洗了个澡」，AI 规范化后创建「睡觉」「洗澡」。2-gram 匹配不到，白白重试一次 DeepSeek API 调用。

### 根因 4：PM2 频繁重启 — 无全局错误捕获

**代码位置**：`server.js:697-699`。

server.js 没有 `process.on('uncaughtException')` 和 `process.on('unhandledRejection')`，任何未捕获异常都会导致进程退出。PM2 自动拉起，所以用户感知不到崩溃，但重启次数（12 次/37h）说明有隐藏 bug 在反复触发。

### 根因 5：extractJson 解析健壮性不足

**代码位置**：`server.js:454-481` extractJson 函数。

已经处理了 BOM (`0xFEFF`) 和 code block 包裹，但有些不可见字符（零宽空格 `\u200B`、其他 Unicode 空白等）`trim()` 处理不了，会导致第一步 `JSON.parse` 失败。

### 根因 6（次要）：AI 将「看小说到十二点半」误判为午夜

用户说「昨天看小说到十二点半，然后出门吃饭买东西到两点半」——这里是中午 12:30。但 AI 在缺乏睡眠记录上下文的情况下（早上那轮的睡觉记录已被覆盖丢失），把 12:30 理解成了午夜 00:30，创建了一条错误的 看小说 00:00-00:30 记录。属于上下文缺失导致的次生问题。

## 需修复的代码

| 优先级 | 文件 | 函数/位置 | 改什么 |
|--------|------|----------|--------|
| P0 | index.html | saveDayToAPI / executeAction | 跨日期保存前，先从服务端加载目标日期的已有数据并合并，不能直接用内存中的部分数据覆盖 |
| P0 | index.html | updateRecord (1457-1478) | 跨日期搜到记录时，在原地更新，不要搬移到 dateStr。只有当 update data 里显式给了 date 字段时才搬 |
| P1 | server.js | validateCreates (436-452) | 短标题（≤3 字）放宽条件：单字匹配或语义宽松匹配；或干脆把验证阈值从「所有 2-gram 都要匹配」改为「标题中的字符有 ≥50% 在消息中出现」 |
| P2 | server.js | 全局 | 加 `process.on('uncaughtException', ...)` 和 `process.on('unhandledRejection', ...)`，记录崩溃堆栈 |
| P3 | server.js | extractJson (454-481) | 加 `replace(/[\u200B\u200C\u200D\uFEFF]/g, '')` 清理零宽字符 |

---

## 2026-08-21 追加：改名部署后 AI 对话崩溃

### 现象

改名「流水账」并部署新版（含日记/周报后端迁移）后，AI 对话接口反复报：

```
[FATAL] unhandledRejection: TypeError: Cannot read properties of undefined (reading 'length')
    at loadMultiDayContext (server.js:97)
```

### 根因

A/B 迁移新增了 `diary.json`（手写日记）和 `weekly_reports.json`（周报）两个文件存在用户目录。`loadMultiDayContext` 用宽松过滤 `f.endsWith('.json') && f !== 'profile.json' && f !== 'tasks.json'` 收集日期文件，把 `diary.json` / `weekly_reports.json` 也当成"某天的数据"读入，`loadDay` 读出的对象没有 `records` 字段 → 访问 `day.records.length` 崩溃。`/api/days` 端点有同样问题（会把日记/周报错误地作为"天"返回）。

### 修复

两处过滤都改为只认日期格式文件：

```javascript
.filter(f => /^\d{4}-\d{2}-\d{2}\.json$/.test(f))
```

- `server.js` loadMultiDayContext（AI 对话上下文）
- `server.js` /api/days（天列表）

### 验证

- 修复后重启 pm2，重启后无新 FATAL（旧错误记录停在重启前）
- 扫描 24 个 day 文件全部正常，diary/weekly_reports 被正确排除
- 教训：过滤 `endsWith('.json')` 太宽松，新增同目录 .json 文件时必须用日期正则白名单，避免误读
